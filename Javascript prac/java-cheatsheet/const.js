// const = a variable that can't be changed 
// Adds security in your code 


const PI = 3.14159; //the variable can't be changed * consts are usually all uppercase  PI, STUDENTS, etc
let radius;
let circumference;

radius = window.prompt("Enter circle radius");
radius = Number(radius);

circumference = 2 * pi * radius;

console.log("The cicumference is:", circumference);
